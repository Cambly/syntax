---
"@cambly/syntax-core": patch
---

Secondary buttons on a light background now use a black outline instead of `cambio-gray-870`. Applies to `Button`, `LinkButton`, `IconButton` and `IconLinkButton` at every size; the dark-background, `success-secondary` and `destructive-secondary` outlines are unchanged.
