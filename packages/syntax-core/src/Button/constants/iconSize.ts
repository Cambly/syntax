import styles from "../Button.module.css";

export const materialIconSize = {
  sm: styles.smIcon,
  md: styles.mdIcon,
  lg: styles.lgIcon,
};

export const internalIconSize = {
  sm: 100,
  md: 200,
  lg: 300,
} as const;
